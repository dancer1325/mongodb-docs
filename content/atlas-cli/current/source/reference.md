# Reference

You can pass a `.json` configuration file to Atlas CLI commands that accept the `--file` option.

## Section Overview
### JSON

Learn how to pass configuration values to the Atlas CLI with the `--file` option.

- [JSON Configuration Files](/reference/json)
